---
import DocsLayout from '../../../layouts/DocsLayout.astro';

const title = 'Quickstart';
---

<DocsLayout title={title}>
  <h1>{title}</h1>
  <p>This is a quickstart showing how to use this repo as a docs site.</p>

  <h2>Adding docs</h2>
  <p>Add new <code>.astro</code> or <code>.md</code> pages under <code>src/pages/en/docs/</code> or manage content via <code>src/content</code> collections.</p>
</DocsLayout>
